using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmbientNPCPenguin : MonoBehaviour
{
	[SerializeField]
	float _randerRadius = 5f;
	
	[SerializeField]
	float _idleTime = 4f;
	
	
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
