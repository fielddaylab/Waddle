using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BowlingSlope : MonoBehaviour
{
	[SerializeField]
	GameObject _slopeTrigger;
	public GameObject SlopeTrigger => _slopeTrigger;
	
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
